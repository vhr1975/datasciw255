apiVersion: v1
kind: Service
metadata:
  name: project
  namespace: victorramirez
  labels:
    app: project
spec:
  type: LoadBalancer
  ports:
  - nodePort: 30080
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: project
